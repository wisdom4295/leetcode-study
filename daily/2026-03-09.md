# 📅 2026-03-09 (월) — LeetCode 학습 일지

## 📌 전체 플랜 진행 현황

| 주차 | 기간 | 주제 | 진행 |
|------|------|------|------|
| 1주차 | 3/9 ~ 3/13 | Array, String, Hash Table | 🟩 1/5일 완료 |
| 2주차 | 3/16 ~ 3/20 | Stack, Linked List, Binary Search | ⬜ 미시작 |
| 3주차 | 3/23 ~ 3/27 | Tree, Graph | ⬜ 미시작 |
| 4주차 | 3/30 ~ 3/31 | DP, Heap, Trie | ⬜ 미시작 |

---

## 🗓️ 오늘의 풀이 기록

> 풀이 시간: **08:30 ~ 10:16** (총 1시간 46분)

---

<details>
<summary><strong>1. Two Sum</strong> — Easy · Array, HashMap · ⏱️ 25분 · 해설참고: N</summary>

<br>

- 🔗 링크: https://leetcode.com/problems/two-sum/

**문제 요구사항**
- 정수 배열 `nums`와 목표값 `target`이 주어질 때 두 수의 합이 `target`이 되는 두 인덱스 반환
- 같은 인덱스를 두 번 사용할 수 없음, 정확히 하나의 답이 존재한다고 가정

**핵심 패턴**
> 인덱스 저장 HashMap, 보완값(target - nums[i]) 탐색

**내 풀이 코드**
```javascript
function twoSum(nums: number[], target: number): number[] {
    const returnArray = [];
    for(let i=0; i<nums.length; i++){
       for(let j=0; j<nums.length; j++){
            if(i !==j && target-nums[i] === nums[j]){
                 returnArray.push(i)
                 returnArray.push(j)
                 return returnArray
            }
       }
    }
    
};

// return : 인덱스 값을 2개 가진 배열
// num 배열의 인자값의 합이 target의 값과 같을때 return 배열에 인덱스를 넣는 것, 동일한 인자는 갖을 수 없음
// 0번째 index의 값을 잡고 target - index 했을때 값이 해당 배열에 있으면 그 값을 넣기
// returnArray = [];
// index를 for문으로 돌면서 i인덱스가 0일때 두번째 for문 j인덱스가 target-num[i] === 0이면 
// returnArray에 2개의 인덱스 값을 push 
```

**다른 풀이 방법**
```javascript
function twoSum(nums: number[], target: number): number[] {

    let m = new Map<number,number>

    for(let i = 0; i<nums.length; i++){
        const c = target - nums[i]
        if(m.has(c)) return [m.get(c),i]

        m.set(nums[i],i)
    }
    
};
```

> 💡 내 풀이와의 차이점:
> -

**잘한 점**
-

**잘못한 점 / 개선할 점**
-

</details>

---

<details>
<summary><strong>2. Best Time to Buy and Sell Stock</strong> — Easy · Array · ⏱️ 25분 · 해설참고: N</summary>

<br>

- 🔗 링크: https://leetcode.com/problems/best-time-to-buy-and-sell-stock/

**문제 요구사항**
- 주가 배열 `prices`가 주어질 때 한 번만 사고팔 수 있을 때 최대 이익 반환
- 이익을 낼 수 없으면 0 반환

**핵심 패턴**
> 최솟값 갱신하며 최대 이익 계산 (Greedy)

**내 풀이 코드**
```javascript
// function maxProfit(prices: number[]): number {
//     let returnBenefit = 0;
//     for(let i=0; i < prices.length; i++){
//         for(let j=1; j < prices.length; j++){
//             if(Math.max(...prices) - Math.min(...prices) >= returnBenefit){
//                 return returnBenefit
//             }
//         }
//     }
// };

function maxProfit(prices: number[]): number {
    let returnBenefit = 0;
    for(let i=0; i < prices.length; i++){
        for(let j=1; j < prices.length; j++){
            if(prices[j] - prices[i] >= returnBenefit){
                returnBenefit = prices[j] - prices[i]
                return returnBenefit
            }
        }
    }
};
// returnBenefit: prices배열의 차가 가장 큰 값을 구하되 첫번째 거래가 2번째 거래되는 값보다 작아야 성립, 차가 0이면 return 0 차가 더 크면 returnBenefit 값을 다시 할당
```

**다른 풀이 방법**
```javascript
// 참고하고 싶은 코드 여기에 작성
```

> 💡 내 풀이와의 차이점:
> -

**잘한 점**
-

**잘못한 점 / 개선할 점**
-

</details>

---

<details>
<summary><strong>3. Contains Duplicate</strong> — Easy · Array, Hash Table · ⏱️ 25분 · 해설참고: N</summary>

<br>

- 🔗 링크: https://leetcode.com/problems/contains-duplicate/

**문제 요구사항**
- 정수 배열 `nums`에서 같은 값이 두 번 이상 등장하면 `true` 반환
- 모든 값이 다르면 `false` 반환

**핵심 패턴**
> Set으로 중복 체크 (Set.size vs array.length 비교)

**내 풀이 코드**
```javascript
function containsDuplicate(nums: number[]): boolean {
    let returnBoolean = false;
    for(let i=0; i < nums.length; i++){
        for(let j=1; j < nums.length; j++){
            if(i !== j && nums[i] === nums[j]){
                return returnBoolean = true
            }
            return returnBoolean
        }
    }
};

// returnBoolean : 
// 배열에서 첫번째 값과 두번째 값이 같으면 true
```

**다른 풀이 방법**
```javascript
// 참고하고 싶은 코드 여기에 작성
```

> 💡 내 풀이와의 차이점:
> -

**잘한 점**
-

**잘못한 점 / 개선할 점**
-

</details>

---

<details>
<summary><strong>4. Valid Anagram</strong> — Easy · String, Hash Table · ⏱️ 25분 · 해설참고: N</summary>

<br>

- 🔗 링크: https://leetcode.com/problems/valid-anagram/

**문제 요구사항**
- 두 문자열 `s`, `t`가 주어질 때 `t`가 `s`의 애너그램이면 `true` 반환
- 길이가 다르면 즉시 `false`

**핵심 패턴**
> 문자 빈도수 HashMap 비교 (s는 +1, t는 -1 후 0 확인)

**내 풀이 코드**
```javascript
function charCount(str) {
  const obj = {};

  for (const char of str) {
    obj[char] = (obj[char] ?? 0) + 1;
  }

  return obj;
}

function isAnagram(s: string, t: string): boolean {
    let returnBoolean = false;
    if(JSON.stringify(charCount(s), Object.keys(charCount(s)).sort()) ===
  JSON.stringify(charCount(t), Object.keys(charCount(t)).sort())) returnBoolean = true;
  return returnBoolean
};

// returnBoolean
// middelObject ={};
// 배열을 순환하면서 해당 값이 있으면 middelObject의 해당 키값에 +1을 하고 
// 그렇지 않으면 해당 키값이 키로 할당되고 수치는 +1로 값을 할당해라
// middelObject의 리턴값이 서로 같으면 returnBoolean의 true를 할당해라
```

**다른 풀이 방법**
```javascript
function isAnagram(s: string, t: string): boolean {
    // return s.split('').sort().join('') === t.split('').sort().join('');

    let sc = Array(26).fill(0);
    let tc = Array(26).fill(0);
    for (let i = 0; i < s.length; ++i) {
        ++sc[s.charCodeAt(i) - 97];
    }
    for (let i = 0; i < t.length; ++i) {
        ++tc[t.charCodeAt(i) - 97];
    }
    return sc.join() === tc.join();
};
```

> 💡 내 풀이와의 차이점:
> -

**잘한 점**
-

**잘못한 점 / 개선할 점**
-

</details>

---

## 🧠 오늘의 패턴 요약

| 문제 | 패턴 |
|------|------|
| Two Sum | 인덱스 저장 HashMap, 보완값 탐색 |
| Best Time to Buy and Sell Stock | 최솟값 갱신하며 최대 이익 계산 |
| Contains Duplicate | Set으로 중복 체크 |
| Valid Anagram | 문자 빈도수 HashMap 비교 |
